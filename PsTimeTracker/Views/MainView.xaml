<Window x:Class="PSTimeTracker.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PSTimeTracker" xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:test="clr-namespace:PSTimeTracker.Testing"
        mc:Ignorable="d"
        Title="Photoshop Time Tracker"  
        Name="mainWindow"
        Width="290" MinWidth="290"
        Height="130" MinHeight="130"
        SizeToContent="Height"
        WindowStyle="None" ResizeMode="NoResize" AllowsTransparency="True"
        Background="{x:Null}"
        d:DataContext="{d:DesignInstance Type=test:MockViewModel, IsDesignTimeCreatable=True}" >

    <Window.Resources>

        <!--#region ScrollBar Style -->
        <Style x:Key="ScrollThumbs" TargetType="{x:Type Thumb}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Grid x:Name="Grid">
                            <Rectangle HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Width="Auto" Height="Auto" Fill="Transparent" />
                            <Border x:Name="ScrollBarRectangle" CornerRadius="5" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Width="Auto" Height="Auto"  Background="{TemplateBinding Background}" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Tag" Value="Horizontal">
                                <Setter TargetName="ScrollBarRectangle" Property="Width" Value="Auto" />
                                <Setter TargetName="ScrollBarRectangle" Property="Height" Value="7" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type ScrollBar}">
            <Setter Property="Margin" Value="-10,0,-17,0" />
            <Setter Property="Stylus.IsFlicksEnabled" Value="False" />
            <Setter Property="Foreground" Value="#FFA1A1A1" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Width" Value="8" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid x:Name="GridRoot" Width="8" Background="{TemplateBinding Background}">
                            <Track x:Name="PART_Track" IsDirectionReversed="true" Focusable="false">
                                <Track.Thumb>
                                    <Thumb x:Name="ScrollBarThumb" Background="{TemplateBinding Foreground}" Style="{StaticResource ScrollThumbs}" />
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton x:Name="PageUp" Command="ScrollBar.PageDownCommand" Opacity="0" Focusable="false" />
                                </Track.IncreaseRepeatButton>
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton x:Name="PageDown" Command="ScrollBar.PageUpCommand" Opacity="0" Focusable="false" />
                                </Track.DecreaseRepeatButton>
                            </Track>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter TargetName="ScrollBarThumb" Property="Visibility" Value="Collapsed" />
                            </Trigger>
                            <Trigger Property="Orientation" Value="Horizontal">
                                <Setter TargetName="GridRoot" Property="LayoutTransform">
                                    <Setter.Value>
                                        <RotateTransform Angle="-90" />
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="PART_Track" Property="LayoutTransform">
                                    <Setter.Value>
                                        <RotateTransform Angle="-90" />
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="Width" Value="Auto" />
                                <Setter Property="Height" Value="8" />
                                <Setter TargetName="ScrollBarThumb" Property="Tag" Value="Horizontal" />
                                <Setter TargetName="PageDown" Property="Command" Value="ScrollBar.PageLeftCommand" />
                                <Setter TargetName="PageUp" Property="Command" Value="ScrollBar.PageRightCommand" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>               
                
            </Setter>

            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="Red"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!--#endregion-->

        <!-- Gradient -->
        <LinearGradientBrush x:Key="MainGradient" StartPoint="0.5, 0" EndPoint="0.5, 400" 
                             MappingMode="Absolute" ColorInterpolationMode="ScRgbLinearInterpolation">
            <GradientStop Color="{StaticResource GradientTop}" Offset="0"/>
            <GradientStop Color="{StaticResource GradientBottom}" Offset="1"/>
        </LinearGradientBrush>

        <!--#region Header Buttons -->

        <sys:Double x:Key="HeaderButtonsDefaultOpacity">0.45</sys:Double>
        <sys:Double x:Key="HeaderButtonsHoverOpacity">0.9</sys:Double>

        <Storyboard x:Key="ButtonMouseOverAnimation">
            <DoubleAnimation Duration="0:0:0.08" 
                             Storyboard.TargetProperty="Opacity"
                             To="{StaticResource HeaderButtonsHoverOpacity}" />
        </Storyboard>

        <Storyboard x:Key="ButtonMouseLeaveAnimation">
            <DoubleAnimation Duration="0:0:0.35" 
                             Storyboard.TargetProperty="Opacity"
                             To="{StaticResource HeaderButtonsDefaultOpacity}" />
        </Storyboard>

        <Style x:Key="HeaderImageButton" TargetType="Image">
            <Setter Property="Width" Value="12"/>
            <Setter Property="Height" Value="12"/>
            <Setter Property="Opacity" Value="{StaticResource HeaderButtonsDefaultOpacity}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard Storyboard="{StaticResource ButtonMouseOverAnimation}"/>
                    </Trigger.EnterActions>

                    <Trigger.ExitActions>
                        <BeginStoryboard Storyboard="{StaticResource ButtonMouseLeaveAnimation}"/>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="Menu" TargetType="Image" BasedOn="{StaticResource HeaderImageButton}">
            <Setter Property="Source" Value="/Resources/Images/menu_24.png"/>
            <Setter Property="Width" Value="16"/>
            <Setter Property="Height" Value="16"/>
        </Style>

        <Style x:Key="Minimize" TargetType="Image" BasedOn="{StaticResource HeaderImageButton}">
            <Setter Property="Source" Value="/Resources/Images/minimize_24.png"/>
        </Style>

        <Style x:Key="Close" TargetType="Image" BasedOn="{StaticResource HeaderImageButton}">
            <Setter Property="Source" Value="/Resources/Images/close_24.png"/>
        </Style>

        <!--#endregion-->
        
        <!--#region ListView Style -->

        <Style x:Key="TextBlockStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{StaticResource RobotoLight}"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Foreground" Value="{StaticResource HeaderForegroundBrush}"/>
            <Setter Property="Margin" Value="3 0"/>
        </Style>

        <Style x:Key="ItemBorderStyle" TargetType="{x:Type Border}">
            <Setter Property="Background" Value="{StaticResource HeaderBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource HeaderBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0 1.5"/>

            <Style.Triggers>

                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#6088ccff"/>
                </Trigger>

                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                    <Setter Property="Background" Value="#aa66ccff"/>
                </DataTrigger>

            </Style.Triggers>
        </Style>

        <Style x:Key="ItemStyle" TargetType="{x:Type ListViewItem}">
            <Setter Property="IsSelected" Value="{Binding IsSelected}"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Background" Value="LightCyan"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListViewItem}">
                        <StackPanel>
                            <ContentPresenter/>
                        </StackPanel>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <DataTemplate x:Key="ItemTemplate">
            <Border Style="{StaticResource ItemBorderStyle}">

                <Grid Background="{x:Null}" Margin="2" >

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="3*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                           Text="{Binding FileName}" 
                           HorizontalAlignment="Left"
                           
                           Style="{StaticResource TextBlockStyle}"/>

                    <TextBlock Grid.Column="1"
                           HorizontalAlignment="Right"
                           Text="{Binding TrackedSeconds, Converter={StaticResource SecondsToTimeConverter}}"
                           Style="{StaticResource TextBlockStyle}"/>

                </Grid>

            </Border>
           
        </DataTemplate>

        <!--#endregion-->
        
    </Window.Resources>

    <Border Name="MainBorder"
            Margin="15"
            Background="{StaticResource MainGradient}"
            BorderBrush="{StaticResource MainBorderBrush}" 
            BorderThickness="1.1"
            SnapsToDevicePixels="False">
        <Border.Effect>
            <DropShadowEffect BlurRadius="12" ShadowDepth="3" Direction="-90" Opacity="0.3"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

                
            <!--#region Header -->
            
            <Grid Name="HeaderContainer" 
                  Margin="0"
                  Background="{StaticResource HeaderBackgroundBrush}">

                <Rectangle Fill="{StaticResource HeaderBorderBrush}" 
                           Height="1" Margin="0 -1"
                           SnapsToDevicePixels="False" VerticalAlignment="Bottom"/>

                <TextBlock Name="SummaryTime" HorizontalAlignment="Center" VerticalAlignment="Center"
                           FontFamily="{StaticResource MontserratRegular}"
                           FontSize="22" Foreground="{StaticResource HeaderForegroundBrush}"
                           Text="{Binding SummarySeconds, Converter={StaticResource SecondsToTimeConverter}}" 
                           d:Text="00:00:13"/>

                <TextBlock Name="ItemsCount" HorizontalAlignment="Center" Margin="-20"
                           Text="{Binding ItemsCount}" d:Text="4 files"/>


                <StackPanel Name="LeftHeaderButtonsContainer" Orientation="Horizontal" HorizontalAlignment="Left">

                    <Image Style="{StaticResource Menu}"
                           Margin="10 0 0 0"/>

                </StackPanel>


                <StackPanel Name="RightHeaderButtonsContainer" Orientation="Horizontal" HorizontalAlignment="Right">

                    <Image Style="{StaticResource Minimize}"
                           Margin="0 0 5 0"/>
                    <Image Style="{StaticResource Close}"
                           Margin="5 0 8 0"
                           MouseLeftButtonDown="Close_LeftButtonDown"/>

                </StackPanel>

            </Grid>


        <!--#endregion-->

            

            <!-- Files -->

                <ListView Name="MainListView"
                          Grid.Row="1"
                          Margin="4"
                          ItemsSource="{Binding PsFilesList}" d:ItemsSource="{Binding Files}"
                          Background="{x:Null}" BorderBrush="{x:Null}"
                          VerticalAlignment="Top"                          
                          HorizontalContentAlignment="Stretch"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                          Height="auto"                          
                          ItemContainerStyle="{StaticResource ItemStyle}"
                          ItemTemplate="{StaticResource ItemTemplate}">
                    
                    
                    
                </ListView>

            <TextBlock Grid.Row="1"
                       Text="Empty" 
                       Style="{StaticResource TextBlockStyle}"
                       VerticalAlignment="Center" HorizontalAlignment="Center"
                       Visibility="{Binding ListIsEmpty, Converter={StaticResource BoolToVisibilityConverter}}"
                       d:Visibility="Collapsed"/>


            <Grid Background="{StaticResource MainGradient}"
                  Grid.Row="1"
                  Visibility="{Binding CanRestorePreviousList, Converter={StaticResource BoolToVisibilityConverter}}"
                  d:Visibility="Hidden">
                <local:RestoreDialog Grid.Row="1"/>
            </Grid>

        </Grid>
    </Border>
</Window>
